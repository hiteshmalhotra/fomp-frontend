import { defineConfig, devices } from '@playwright/test'

/**
 * E2E smoke suite. Backend APIs are mocked via page.route so the suite
 * runs without the Java stack — it validates the frontend's real behavior
 * against the documented API contracts.
 * Run: npm run e2e  (starts the Vite dev server automatically)
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: process.env.CI ? 'github' : 'list',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
})
